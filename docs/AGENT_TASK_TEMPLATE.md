# Шаблон задачи для агента (DisignApril)

Скопируй блок ниже в задачу/чат и заполни поля.

~~~markdown
## Мета
- ID / ветка: (например `DS-012`, `feature/ds-token-audit`)
- Приоритет: low | medium | high
- Связанные документы: (TASK, PR, issue, section in `DESIGN_SYSTEM.md`)

## Цель
Коротко: какое изменение нужно получить в дизайн-системе и зачем.

## Контекст
- Репозиторий: `DisignApril`
- Затрагиваемые области: `packages/tokens` | `packages/ui` | `apps/showcase` | `docs`
- Важные файлы: @path/to/file

## Входит в объем
- ...
- ...

## Не входит в объем
- ...
- ...

## Ограничения
- Следовать архитектуре текущего монорепозитория на pnpm.
- Не импортировать `UIKit` в production-примеры использования.
- При изменении токенов и правил обязательно обновить документацию.

## Критерии готовности
- [ ] Кодовые изменения внесены.
- [ ] Проверки проходят (`pnpm lint`, `pnpm typecheck`, `pnpm build`).
- [ ] Обновлены нужные документы.

## Проверка
```bash
pnpm lint
pnpm typecheck
pnpm build
```

## Формат отчета
`tasks/<id>/REPORT.md` по шаблону `docs/AGENT_REPORT_TEMPLATE.md`.
~~~
